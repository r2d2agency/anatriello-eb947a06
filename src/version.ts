// Versão atual do sistema.
// Formato: epoch em segundos no momento do build (sempre crescente entre deploys).
// Atualizado automaticamente por scripts/bump-version.mjs a cada `npm run build`.
export const APP_VERSION = "0";
